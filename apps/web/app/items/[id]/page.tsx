// TODO: CRUD detail view. Server Component that calls inventory-service's
// GetItem RPC (and, for mutations, UpdateItem/DeleteItem via server
// actions) using the server-side gRPC client. Params contain the item id.
export default function ItemDetailPage() {
  return null;
}
