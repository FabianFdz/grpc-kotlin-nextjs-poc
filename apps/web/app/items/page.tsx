// TODO: CRUD list view. Server Component that calls inventory-service's
// ListItems RPC via a server-side gRPC client (@grpc/grpc-js, built from
// @poc/contracts types) and renders the items with links to /items/[id].
export default function ItemsListPage() {
  return null;
}
