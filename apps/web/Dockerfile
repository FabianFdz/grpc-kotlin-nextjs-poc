# TODO: build the Next.js app (pnpm install --frozen-lockfile && pnpm build)
# and run it with `next start`. Referenced by docker-compose.yml's web
# service. No implementation yet — this file exists so the compose build
# context is valid.
