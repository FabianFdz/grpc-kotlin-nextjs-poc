# TODO: build inventory-service with a Gradle build stage (Kotlin JVM) and
# run it on a slim JRE base image. Referenced by docker-compose.yml's
# inventory-service service. No implementation yet — this file exists so
# the compose build context is valid.
