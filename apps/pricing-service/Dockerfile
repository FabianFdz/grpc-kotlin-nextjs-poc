# TODO: build pricing-service with a Gradle build stage (Kotlin JVM) and run
# it on a slim JRE base image. Referenced by docker-compose.yml's
# pricing-service service. No implementation yet — this file exists so the
# compose build context is valid.
